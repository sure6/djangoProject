from django.apps import AppConfig

# app registration
class PollsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'polls'
    verbose_name = "vote investigation"
